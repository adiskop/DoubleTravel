<%= form_for(@visited_gem) do |f| %>

<%= render partial: 'layouts/errors', locals:{object: @visited_gem} %>

    <%= f.label :name %>
    <%= f.text_field :name %>

     <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.hidden_field :user_id %>
  <br><br>
  <%= f.label :destination_id, "Select a Destination" %>
  <%= f.collection_select :destination_id, Destination.all, :id, :name, prompt: true %>
  <br><br>
    <%= f.submit %>
  <%end%>

